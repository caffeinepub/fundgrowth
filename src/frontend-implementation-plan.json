{
  "kind": "implementation_plan",
  "version": "1.0",
  "title": "Add Gold offerings section to Bonds page",
  "requirements": [
    {
      "id": "REQ-1",
      "summary": "Add a static Gold product list section beneath the existing bond catalog grid on the /bonds page with exactly 5 offerings and their provided detail strings (no emojis), styled responsively for light/dark themes.",
      "acceptanceCriteria": [
        "Visiting /bonds shows an additional section beneath the existing bond catalog area (below the main grid with filters + bond cards).",
        "The section renders 5 items with the exact names and the exact detail strings specified (including punctuation, spacing around pipes, and currency symbol ₹).",
        "No emojis are displayed in this section; use standard UI elements (e.g., bullet, dot, badge) instead of 🟡.",
        "The section displays correctly in both light and dark themes and is responsive on mobile and desktop."
      ],
      "file_operations": [
        {
          "path": "frontend/src/components/bonds/GoldOfferingsSection.tsx",
          "operation": "create",
          "description": "Create a reusable, static Gold offerings section component that renders the 5 offerings (title + exact detail string) in a responsive layout using existing Tailwind/theme tokens (e.g., text-foreground, text-muted-foreground, bg-card, border) and a non-emoji marker (e.g., dot/bullet). Verify the component's usage instructions before implementing."
        },
        {
          "path": "frontend/src/pages/BondsPage.tsx",
          "operation": "modify",
          "description": "Render the new GoldOfferingsSection below the existing bond catalog grid (after the filters + bond cards area) on the /bonds page, ensuring spacing and responsiveness without changing existing bond catalog behavior. Verify the component's usage instructions before implementing."
        }
      ]
    }
  ]
}